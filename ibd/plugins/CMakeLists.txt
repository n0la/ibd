CMAKE_MINIMUM_REQUIRED(VERSION 3.0)

INCLUDE_DIRECTORIES(${IRC_INCLUDE_DIRS})

ADD_EXECUTABLE("nickserv" "nickserv.c")
TARGET_LINK_LIBRARIES("nickserv" ${IRC_LIBRARIES})
INSTALL(TARGETS "nickserv" DESTINATION libexec/ibd/)

SET(PLUGINS "log")
INSTALL(PROGRAMS ${PLUGINS} DESTINATION libexec/ibd/)
